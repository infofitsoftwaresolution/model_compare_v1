# AWS Configuration
# Copy this file to .env and fill in your AWS credentials
# DO NOT commit .env to version control!

# AWS Access Credentials (Option 1: Direct credentials)
AWS_ACCESS_KEY_ID=your_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_secret_access_key_here

# AWS Region
AWS_REGION=us-east-2

# AWS Profile (Option 2: Use AWS Profile instead of direct credentials)
# If using AWS Profile, leave AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY empty
# AWS_PROFILE=your-aws-profile-name
